# Historical Supabase implementation

Preserved from baseline `a6ee904` when the active application moved to Convex.
These files are not imported, built, deployed or tested by the active application.
The original migrations and acceptance tests are retained for audit and rollback.
Use the complete baseline commit in a separate checkout to run the old stack;
this archive alone is not a standalone application. The old hosted Supabase
project has not been deleted or reset. No real business data was migrated.

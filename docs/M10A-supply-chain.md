# Supply-chain and release provenance

The npm lockfile is authoritative; use `npm ci`, never an unreviewed lockfile rewrite. Pinned `@convex-dev/auth@0.0.95` has a narrowly scoped deterministic postinstall patch in `scripts/patch-convex-auth.mjs`: expected original hashes/layout and package version are checked before patching; unexpected upstream changes fail closed. The patch does not fetch executable content. Clean install must exercise this check, not rely on an already patched working tree.

Review inventory: Next/React and Convex serve production traffic; Convex Auth/Auth core implement identity; Zod validates; Svix authenticates webhook content; Radix/Tailwind/Lucide form UI. TypeScript, ESLint, Prettier, Playwright, Vitest, convex-test and tsx are development tooling. Record exact resolved versions from `npm ls --depth=0` and the lockfile SHA in the acceptance artifact. Do not auto-upgrade auth/Convex to clear audit output without compatibility and security regressions.

At this checkpoint `npm audit --json` reported no advisories in any severity category. This is a point-in-time registry result, not proof of absence of vulnerabilities. Re-run before release and after lockfile/security changes; triage exploitability and runtime exposure without suppressing unexplained high/critical findings. Proposed evidence expiry is seven days.

A read-only GitHub Actions quality workflow now exists, pinned to immutable checkout/setup-node revisions, with no secrets, deployment step or privileged PR trigger. The HEAD-start quality run 35160813047 completed successfully under GitHub actor daryan-kf. The GitHub branch-protection endpoint reported that main is not protected; required protection remains an explicit M10B prerequisite. Final candidate CI is recorded separately. Before M10B, a named GitHub administrator must verify protected main, required review/checks, least-privilege scoped deploy identity, protected production environments and no secrets exposed to untrusted pull requests. Any later workflow must pin third-party actions by immutable revision, use read-only token defaults and explicit restricted deployment jobs; do not use pull_request_target to run untrusted code with secrets.

The latest clean install/build and bundle inspection are recorded in `M10A-final-results.json` with exact source provenance. Production source maps are not intentionally published; verify `.next/static` contains no maps and no private environment/provider credential values before promotion. Public Convex URLs are configuration, not credentials. Runtime/browser and external environment verification remain separate. A release comprises the source commit, lockfile digest, build/check results, approved schema-change inventory and rollback compatibility review. Never equate a successful build with permission to deploy production.

Fresh install also reports upstream deprecations for transitive Lucia/Oslo packages and the pinned ESLint release. An advisory-free audit does not resolve maintenance/support risk. Review the Convex Auth dependency/support path and toolchain upgrade separately; no emergency untested upgrade or risk waiver is applied.

## Maintenance review, 2026-09-17 UTC

The official [Convex Auth page](https://docs.convex.dev/auth/convex-auth) still identifies the library as beta and Next.js server integration as under active development. The pinned release and deterministic patch are retained for compatibility and tested here; this is not a support guarantee. Transitive Lucia/Oslo deprecation is recorded as a maintenance dependency of the selected auth package, not silently removed or called a passed MFA solution. Before M10B, the Security/Technical Lead must approve a supported privileged-MFA identity architecture and its upgrade/migration regression plan (AUTH-MFA), including recovery, issuer/session compatibility and rollback. ESLint is development-only; its upgrade requires lint-rule compatibility review, not an emergency runtime substitution. No high/critical advisory was waived and no dependency was automatically upgraded.

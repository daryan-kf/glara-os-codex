name: Quality gates
on:
  pull_request:
  push:
    branches: [main]
permissions:
  contents: read
concurrency:
  group: quality-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  quality:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      NEXT_TELEMETRY_DISABLED: "1"
      GLARA_ENVIRONMENT: development
      M9_EMAIL_ENABLED: "false"
      M9_CALENDAR_ENABLED: "false"
      AUTH_EMAIL_ENABLED: "false"
      GLARA_AI_SECURITY_APPROVED: "false"
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false
          fetch-depth: 0
      - uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: 24.15.0
      - run: npm ci
      - run: npm run typecheck
      - run: npm run lint
      - run: npm run format:check
      - run: npm test
      - run: npm run security:secrets
      - run: npm audit --audit-level=high
      - run: npm run build
      - run: git diff --check
# No provider secrets, deployment credentials, privileged PR trigger, or deployment step.
# Readiness metadata is reviewed independently; a green CI run is not M10A approval.
